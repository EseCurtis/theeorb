# SHARED MIND — cross-agent handover
# Every agent MUST read this at session start and rewrite the sections below at session end.
# Do not delete this file. Do not leave STATUS as UNKNOWN when you exit.

## STATUS
DONE — added a dimensional violet button system to the landing page

## LAST SESSION
- Added a shared `orb-button` class with a violet vertical gradient, bright upper bevel, inset lower shading, depth shadow, and pressed state.
- Applied the new treatment to the header, hero, and release call-to-action buttons.
- Preserved the user’s concurrent hero/logo layout adjustments while applying the button system.
- Restored the typed `next/image` logo wrapper so image props are valid and LCP image handling remains optimized.
- Removed a Google-font network dependency that blocked production builds; typography now uses local system display and monospace fallbacks.

## NEXT
- Define the first release-permission model and default autonomy boundaries.
- Implement the early-access capture destination or replace the mailto call to action when the onboarding route exists.
- Design and implement the MVP Orb creation and Nursery flow from the product docs.

## BLOCKERS
none

## TOUCHED
- web/src/app/globals.css
- web/src/app/layout.tsx
- web/src/components/brand-logo.component.tsx
- web/src/components/landing-call-to-action.component.tsx
- web/src/components/landing-hero.component.tsx
- web/src/components/site-header.component.tsx
- .agent/shared-mind.txt

## COMMANDS
- `pnpm lint` (in `web/`) — pass
- `pnpm build` (in `web/`) — pass
- `git diff --check` — pass
- `.agent/scripts/agen-run.sh .agent/post-agent-run.acmd` — pending final handover commit

## REPO MAP
- `app/` — Capacitor + Vite mobile (Frontend.Mobile.RULES.md)
- `server/` — Express API (Server.RULES.md)
- `web/` — Next.js (Frontend.Web.RULES.md)
- `.agent/project/` — product docs (templates — fill in)
- `.agent/` — code rules, shared-mind, Agen post-run

## CONVENTIONS REMINDER
- `.agent/` = all agents | `.cursor/` = Cursor only (rules + hooks)
- Post-run: update this file, then `.agent/scripts/agen-run.sh .agent/post-agent-run.acmd`
