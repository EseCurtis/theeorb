# SHARED MIND — cross-agent handover
# Every agent MUST read this at session start and rewrite the sections below at session end.
# Do not delete this file. Do not leave STATUS as UNKNOWN when you exit.

## STATUS
DONE — mobile account flow, protected tab shell, and pixel arcade visual system

## LAST SESSION
- Preserved and checkpointed the user’s uncommitted web edits before beginning app work (`8029909`). Do not modify those hero/orb edits unless the user explicitly asks.
- Added a versioned `/api/v1` authentication foundation: sign-up, sign-in, password recovery, password reset, current session, and sign-out. Prisma now has `User` and `PasswordResetToken` models with migration `20260727003000_add_auth`.
- Implemented symmetric account-sensitive responses, dummy bcrypt comparison for unknown users, hashed recovery tokens, Passport JWT session protection, and matching Swagger documentation.
- Added mobile secure-on-native session persistence with Capacitor Preferences, TanStack Query auth hooks, sign-in/sign-up/recovery/reset screens, protected Home and Settings routes, and a fixed Home/Settings tab navigator.
- Home is an intentional empty Nursery state, not fake activity. Settings supports theme selection and local sign-out.
- Recovery token delivery requires an email provider. The API creates a one-time hashed token and returns the same generic accepted response for every email, but it must be wired to an email plugin before recovery links can reach end users. Never log reset tokens.
- Applied the web’s pixel-arcade panel language to the app: sharp physical frames, compact system strips, matrix markers, segmented footers, thematic violet/cyan/pink/amber treatments, and dimensional controls.
- Pixel Digivolve is now used for mobile page headings, action buttons, field labels, tabs, and panel labels; body/form entry text remains on the readable UI face.
- Refined `AuthField` after visual feedback: inputs now use a square inset console-slot frame with a visible required-state label, high-contrast readable entry text, and a focused violet boundary. Do not reintroduce soft rounded input containers.

## NEXT
- Add an email delivery plugin/service for password-recovery links and configure the app’s deep-link handler to open `/auth/reset` with its token.
- Build the first real Nursery/Orb creation flow, then replace the disabled Home CTA.
- Define the release-permission model and default autonomy boundaries.
- The mobile app now bundles `app/public/fonts/PixelDigivolve-mOm9.ttf` and maps it to `var(--font-pixel)` for themed headings and system labels. Keep body and form text on the readable UI font.

## BLOCKERS
- No configured email provider or app recovery deep-link scheme yet, so recovery link delivery is not end-user complete despite the secure reset endpoint and screens being implemented.

## TOUCHED
- server/prisma/schema.prisma
- server/prisma/migrations/20260727003000_add_auth/migration.sql
- server/src/routes/auth.route.ts
- server/src/controllers/auth.controller.ts
- server/src/services/auth.service.ts
- server/src/middleware/jwt.token.ts
- server/src/schemas/auth.schema.ts
- server/swagger/swagger.json
- app/src/app/{sign-in,sign-up,password-recovery,password-reset,home,settings}.screen.tsx
- app/src/components/auth/*
- app/src/components/navigation/*
- app/src/hooks/use-auth.hook.ts
- app/src/shared/{auth-session.util.ts,api/auth.api.ts,types/auth.types.ts}
- app/src/routes/{app.tsx,app/settings.tsx,auth/*}
- app/src/styles.css
- app/public/fonts/PixelDigivolve-mOm9.ttf
- app/src/components/common/{panel,button-primary,button-secondary}.component.tsx
- app/src/components/layout/screen-layout.component.tsx

## COMMANDS
- `DATABASE_URL='file:./dev.db' pnpm prisma migrate deploy` (in `server/`) — pass
- `DATABASE_URL='file:./dev.db' pnpm prisma generate` (in `server/`) — pass
- `pnpm build` (in `server/`) — pass
- Manual HTTP verification of health, sign-up, sign-in, session, invalid credentials, and generic unknown-account recovery — pass
- `pnpm build` (in `app/`) — pass
- `pnpm lint` (in `app/`) — pass
- Verified the bundled Pixel Digivolve font is referenced by `app/src/styles.css` and used by the mobile account and tab labels.
- Used the UI/UX design-system review for pixel-art accessibility, touch targets, responsive layout, and font loading; retained readable body typography and visible labels.
- `git diff --check` — pass

## REPO MAP
- `app/` — Capacitor + Vite mobile (Frontend.Mobile.RULES.md)
- `server/` — Express API (Server.RULES.md)
- `web/` — Next.js (Frontend.Web.RULES.md)
- `.agent/project/` — product docs
- `.agent/` — code rules, shared-mind, Agen post-run

## CONVENTIONS REMINDER
- `.agent/` = all agents | `.cursor/` = Cursor only (rules + hooks)
- Preserve manual user edits. If present at task start or during work, checkpoint the exact state before implementation changes.
- New normal backend routes are versioned under `/api/v1` and must update Swagger.
- Mobile UI composition uses `View` and `Text`; screens call domain hooks, never APIs directly.
- Post-run: update this file, commit handover, then run `.agent/scripts/agen-run.sh .agent/post-agent-run.acmd`.
