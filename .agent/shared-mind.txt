# SHARED MIND — cross-agent handover
# Every agent MUST read this at session start and rewrite the sections below at session end.
# Do not delete this file. Do not leave STATUS as UNKNOWN when you exit.

## STATUS
DONE — replaced brand asset placeholders with the supplied orb brand artwork

## LAST SESSION
- Inspected every image in `.required/brand/raw/` and classified it by visual role.
- Replaced `logo-color.png`, `logo-black.png`, and `logo-white.png` with their matching orb-logo variants.
- Replaced `icon.png`, `notification-icon.png`, `splash.png`, and `splash-dark.png` with the matching app icon, notification icon, and light/dark splash artwork.
- Moved all seven assets out of `.required/brand/raw/`; that directory is now empty.

## NEXT
- Use the renamed assets from `.required/brand/` in application icon and splash generation workflows as needed.

## BLOCKERS
- Git is unavailable in this workspace (`/Users/mac/Desktop/Work/REAL LIFE/theorb` has no `.git` directory), so the required commit checkpoint cannot be created.

## TOUCHED
- .required/brand/icon.png
- .required/brand/logo-black.png
- .required/brand/logo-color.png
- .required/brand/logo-white.png
- .required/brand/notification-icon.png
- .required/brand/splash.png
- .required/brand/splash-dark.png
- .required/brand/raw/* (moved out)
- .agent/shared-mind.txt

## COMMANDS
- `sips -g format -g pixelWidth -g pixelHeight .required/brand/{raw,}/*.png` — pass
- visual contact sheet review of all seven raw assets — pass
- asset replacement and raw-directory count check — pass (0 source files remain)

## REPO MAP
- `app/` — Capacitor + Vite mobile (Frontend.Mobile.RULES.md)
- `server/` — Express API (Server.RULES.md)
- `web/` — Next.js (Frontend.Web.RULES.md)
- `.agent/project/` — product docs (templates — fill in)
- `.agent/` — code rules, shared-mind, Agen post-run

## CONVENTIONS REMINDER
- `.agent/` = all agents | `.cursor/` = Cursor only (rules + hooks)
- Post-run: update this file, then `.agent/scripts/agen-run.sh .agent/post-agent-run.acmd`
