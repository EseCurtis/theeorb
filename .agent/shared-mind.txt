# SHARED MIND — cross-agent handover
# Every agent MUST read this at session start and rewrite the sections below at session end.
# Do not delete this file. Do not leave STATUS as UNKNOWN when you exit.

## STATUS
DONE — mobile auth and onboarding now share a pixel-cosmic background system

## LAST SESSION
- Preserved the user’s prior manual web edits in checkpoint `8029909`; do not alter the landing Hero/Orb composition unless the user requests it.
- Authentication API remains versioned under `/api/v1`, with symmetric account-sensitive responses, Prisma-backed users/recovery tokens, Passport JWT, and Swagger coverage.
- Rebuilt all mobile sign-in, sign-up, recovery, and password-reset screens around a single calm `AuthFormFrame` terminal, replacing the previous scattered rounded form surfaces.
- Auth fields are now straight-edged, high-contrast console slots with visible labels, required markers, and a focused violet state. Do not reintroduce soft rounded fields.
- Added `/onboarding`: a no-fake-metrics, three-step Raise → Release → Observe introduction. Root now redirects here; the final action opens sign-up, while existing members can enter sign-in at any point.
- Pixel Digivolve is the global app typeface, including native form controls. The previous Inter/system body stack must not be restored without explicit direction.
- Removed the Tailwind `font-sans` body class and the code-element system font override. Pixel Digivolve is now the only declared app typeface at every boundary.
- Added a shared `CosmicBackground` behind every auth screen and onboarding: subtle grid mesh, orbital paths, violet/cyan/amber pixel stars, and portal glows. It is decorative and pointer-inert so it cannot interfere with form controls.
- Preserved the user’s concurrent onboarding Orb-logo sizing adjustment (`size-full`) without altering its intent.

## NEXT
- Add an email delivery plugin/service for recovery links, then configure Capacitor deep links to open `/auth/reset` with the recovery token.
- Implement the first real Nursery/Orb creation flow and replace the disabled Home CTA.
- Define the release-permission model and default autonomy boundaries.

## BLOCKERS
- No email provider or app recovery deep-link scheme is configured. Recovery endpoints and screens are complete, but reset links cannot yet reach end users.

## TOUCHED
- app/src/app/onboarding.screen.tsx
- app/src/app/{sign-in,sign-up,password-recovery,password-reset}.screen.tsx
- app/src/components/auth/{auth-shell,auth-form-frame,auth-field}.component.tsx
- app/src/components/common/cosmic-background.component.tsx
- app/src/routes/{index,onboarding}.tsx
- app/src/routeTree.gen.ts
- app/src/styles.css
- server/src/{routes,controllers,services}/auth.*
- server/prisma/migrations/20260727003000_add_auth/migration.sql
- server/swagger/swagger.json

## COMMANDS
- `pnpm build` (in `app/`) — pass
- `pnpm lint` (in `app/`) — pass
- `git diff --check` — pass
- `.agent/scripts/agen-run.sh .agent/post-agent-run.acmd` — pending final handover commit

## REPO MAP
- `app/` — Capacitor + Vite mobile (Frontend.Mobile.RULES.md)
- `server/` — Express API (Server.RULES.md)
- `web/` — Next.js (Frontend.Web.RULES.md)
- `.agent/project/` — product docs
- `.agent/` — code rules, shared-mind, Agen post-run

## CONVENTIONS REMINDER
- Preserve any user/manual changes in an isolated checkpoint before implementation work.
- Normal backend routes must remain under `/api/v1` and update Swagger in the same change.
- Mobile UI composition uses `View` and `Text`; screens call hooks rather than APIs directly.
- Commit milestones, sync each group of seven local commits, then run `.agent/scripts/agen-run.sh .agent/post-agent-run.acmd` before declaring work complete.
