# SHARED MIND — cross-agent handover
# Every agent MUST read this at session start and rewrite the sections below at session end.
# Do not delete this file. Do not leave STATUS as UNKNOWN when you exit.

## STATUS
DONE — Prisma migration history is valid and private Orb setup is ready for local development

## LAST SESSION
- Repaired the local `server/prisma/dev.db` history without deleting data: its schema already contained the two migration results but lacked `_prisma_migrations`, so both committed migrations were marked applied through Prisma.
- Added the standard `server/prisma/migrations/migration_lock.toml`, declaring SQLite. This was the missing repository artifact that prevented Prisma’s migration tooling from determining the connector for a clean database.
- Verified a clean, initialized disposable SQLite database applies both `20260727003000_add_auth` and `20260727010000_add_orb_setup`, and Prisma subsequently reports it up to date.
- The local development database now reports two migrations and is up to date; Prisma schema validation, client generation, and the server TypeScript build all pass.
- The private Orb API and mobile Nursery setup from the previous session remain intact. No autonomous action or release capability is enabled.

## NEXT
- Add editable Orb identity, private teaching/test conversation, and persistent save feedback inside the Nursery.
- Define release permissions, topic/action boundaries, and the visible pause model before building any autonomous behaviour.
- Add an email delivery plugin/service for recovery links, then configure Capacitor deep links to open `/auth/reset` with the recovery token.

## BLOCKERS
- No email provider or app recovery deep-link scheme is configured. Recovery endpoints and screens are complete, but reset links cannot yet reach end users.

## TOUCHED
- app/src/app/{home,orb-setup}.screen.tsx
- app/src/components/{navigation/app-tab-navigator,orb/orb-setup-form}.component.tsx
- app/src/{hooks/use-orb.hook,shared/api/orb.api,shared/types/orb.types}.ts
- app/src/routes/app/nursery.tsx
- app/src/routeTree.gen.ts
- server/prisma/{schema.prisma,migrations/20260727010000_add_orb_setup/migration.sql}
- server/prisma/migrations/migration_lock.toml
- server/src/{controllers/orb.controller,routes/orb.route,schemas/orb.schema,services/orb.service,types/orb.types}.ts
- server/src/index.ts
- server/swagger/swagger.json

## COMMANDS
- `pnpm exec prisma migrate resolve --applied <migration>` (in `server/`) — pass for both committed migrations
- `pnpm exec prisma migrate deploy` and `prisma migrate status` (in `server/`) — pass against local development database
- clean initialized SQLite `prisma migrate deploy` and `prisma migrate status` — pass for both migrations
- `pnpm exec prisma validate`, `prisma generate`, and `pnpm run build` (in `server/`) — pass
- `git diff --check` — pass
- `.agent/scripts/agen-run.sh .agent/post-agent-run.acmd` — pending final handover commit

## REPO MAP
- `app/` — Capacitor + Vite mobile (Frontend.Mobile.RULES.md)
- `server/` — Express API (Server.RULES.md)
- `web/` — Next.js (Frontend.Web.RULES.md)
- `.agent/project/` — product docs
- `.agent/` — code rules, shared-mind, Agen post-run

## CONVENTIONS REMINDER
- Preserve any user/manual changes in an isolated checkpoint before implementation work.
- Normal backend routes must remain under `/api/v1` and update Swagger in the same change.
- Mobile UI composition uses `View` and `Text`; screens call hooks rather than APIs directly.
- Commit milestones, sync each group of seven local commits, then run `.agent/scripts/agen-run.sh .agent/post-agent-run.acmd` before declaring work complete.
