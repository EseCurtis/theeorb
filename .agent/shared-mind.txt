# SHARED MIND — cross-agent handover
# Every agent MUST read this at session start and rewrite the sections below at session end.
# Do not delete this file. Do not leave STATUS as UNKNOWN when you exit.

## STATUS
DONE — added an animated arc-star field and Orb rotation frames to the web landing hero

## LAST SESSION
- Added six decorative four-point stars drawn with curved SVG paths and placed them throughout the landing hero.
- Animated each star independently with warm yellow, orange, pink, violet, and lilac treatments that complement the existing cosmic surface.
- Restored the hero’s active stage and Orb classes, then added a three-frame rotation cycle using the front, left, and right Orb views.
- Added the transparent rotation-frame assets in `web/public/animations/orb-rotation/`; they are used directly by the hero.
- Verified the web app with lint, production build, and whitespace checks.

## NEXT
- Define the first release-permission model and default autonomy boundaries.
- Implement the early-access capture destination or replace the mailto call to action when the onboarding route exists.
- Design and implement the MVP Orb creation and Nursery flow from the product docs.
- A valid `.riv` animation still requires a Rive editor/CLI or existing Rive source. This repository has no Rive dependency, authoring tool, or source file; do not add a placeholder `.riv`.

## BLOCKERS
No local Rive authoring tool, `.riv` source, or Rive project dependency was available. The implemented HTML/CSS animation and PNG keyframes are ready for a later Rive import/export pass.

## TOUCHED
- web/src/components/landing-hero.component.tsx
- web/src/components/hero-stars.component.tsx
- web/src/app/globals.css
- web/public/animations/orb-rotation/orb-rotation-00.png
- web/public/animations/orb-rotation/orb-rotation-28.png
- web/public/animations/orb-rotation/orb-rotation-minus-28.png
- .agent/shared-mind.txt

## COMMANDS
- `pnpm lint` (in `web/`) — pass
- `pnpm build` (in `web/`) — pass
- `git diff --check` — pass
- `.agent/scripts/agen-run.sh .agent/post-agent-run.acmd` — pending final handover commit

## REPO MAP
- `app/` — Capacitor + Vite mobile (Frontend.Mobile.RULES.md)
- `server/` — Express API (Server.RULES.md)
- `web/` — Next.js (Frontend.Web.RULES.md)
- `.agent/project/` — product docs (templates — fill in)
- `.agent/` — code rules, shared-mind, Agen post-run

## CONVENTIONS REMINDER
- `.agent/` = all agents | `.cursor/` = Cursor only (rules + hooks)
- Post-run: update this file, then `.agent/scripts/agen-run.sh .agent/post-agent-run.acmd`
