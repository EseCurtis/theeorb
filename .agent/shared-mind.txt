# SHARED MIND — cross-agent handover
# Every agent MUST read this at session start and rewrite the sections below at session end.
# Do not delete this file. Do not leave STATUS as UNKNOWN when you exit.

## STATUS
DONE — private Gemini-powered Nursery is implemented and verified end-to-end

## LAST SESSION
- Added `20260727065154_add_orb_nursery`: private owner behaviour rules on `Orb` plus durable `OrbLesson` records for owner messages and Orb replies.
- Added protected, versioned Nursery endpoints under `/api/v1/orb/nursery` for private state, private teaching/testing, and behaviour-rule updates. Swagger fully documents the endpoints and response shapes.
- Gemini is called only from `server/src/services/plugins/gemini.service.ts`; `GEMINI_API_KEY` remains server-only. Provider failures surface a generic 503 to the app and retain only safe diagnostics in server logs.
- Corrected the Gemini REST parser to read text from Interactions `steps` rather than the SDK-only `output_text` helper.
- Rebuilt `/app/nursery` into a real workbench with explicit private-DRAFT state, editable owner rules, persisted teaching transcript, and loading/empty/error/saving states. It still cannot release or trigger public actions.
- Verified a full temporary-database flow: sign-up, sign-in, Orb creation, and Gemini Nursery lesson returned HTTP 201 with a non-empty persisted reply. The disposable database was removed afterwards.
- Server build, app lint/build, and Prisma migration application pass. The seven outstanding commits through `a9268e4` were pushed to `origin/master`.

## NEXT
- Define release permissions, topic/action boundaries, and the visible pause model before building any autonomous behaviour.
- Add safe document/memory teaching with explicit source review, deletion, and owner control.
- Add editable Orb identity beyond behaviour rules, including visual form and objective revisions.
- Add an email delivery plugin/service for recovery links, then configure Capacitor deep links to open `/auth/reset` with the recovery token.

## BLOCKERS
- No email provider or app recovery deep-link scheme is configured. Recovery endpoints and screens are complete, but reset links cannot yet reach end users.

## TOUCHED
- app/src/app/{home,orb-setup}.screen.tsx
- app/src/components/{navigation/app-tab-navigator,orb/orb-setup-form,orb/nursery-workbench}.component.tsx
- app/src/{hooks/use-orb.hook,hooks/use-nursery.hook,shared/api/orb.api,shared/types/orb.types}.ts
- app/src/routes/app/nursery.tsx
- app/src/routeTree.gen.ts
- server/prisma/{schema.prisma,migrations/20260727010000_add_orb_setup/migration.sql}
- server/prisma/migrations/migration_lock.toml
- server/prisma/migrations/20260727065154_add_orb_nursery/migration.sql
- server/src/{controllers/nursery.controller,schemas/nursery.schema,routes/nursery.route,services/nursery.service,services/plugins/gemini.service}.ts
- server/src/{services/orb.service,types/orb.types}.ts
- server/src/index.ts
- server/swagger/swagger.json

## COMMANDS
- `pnpm exec prisma migrate dev --name add_orb_nursery` (in `server/`) — pass
- `pnpm run build` (in `server/`) — pass
- temporary-database private Nursery integration flow through Gemini — pass (HTTP 201 with persisted reply)
- `pnpm --dir app lint` and `pnpm --dir app build` — pass
- `git diff --check` — pass
- `.agent/scripts/agen-run.sh .agent/post-agent-run.acmd` — pending final handover commit

## REPO MAP
- `app/` — Capacitor + Vite mobile (Frontend.Mobile.RULES.md)
- `server/` — Express API (Server.RULES.md)
- `web/` — Next.js (Frontend.Web.RULES.md)
- `.agent/project/` — product docs
- `.agent/` — code rules, shared-mind, Agen post-run

## CONVENTIONS REMINDER
- Preserve any user/manual changes in an isolated checkpoint before implementation work.
- Normal backend routes must remain under `/api/v1` and update Swagger in the same change.
- Mobile UI composition uses `View` and `Text`; screens call hooks rather than APIs directly.
- Commit milestones, sync each group of seven local commits, then run `.agent/scripts/agen-run.sh .agent/post-agent-run.acmd` before declaring work complete.
