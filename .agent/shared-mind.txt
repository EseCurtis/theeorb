# SHARED MIND — cross-agent handover
# Every agent MUST read this at session start and rewrite the sections below at session end.
# Do not delete this file. Do not leave STATUS as UNKNOWN when you exit.

## STATUS
DONE — GSAP scroll-motion system added to the landing page

## LAST SESSION
- Added `gsap` and a narrow client-only `LandingScrollMotion` controller; the App Router page remains server-rendered.
- Built scrubbed ScrollTrigger timelines for the Story, Observatory, World, autonomy, and release sections.
- Motion sequences introduce system labels, headings, copy, calls to action, and arcade panels with measured staggering; Observatory and World panels enter from alternating directions for depth.
- Used `data-scroll-*` attributes as motion hooks, without adding CSS selectors or a second styling system.
- Motion is disabled through `prefers-reduced-motion`; content remains present when JavaScript is unavailable.
- Preserved the user-owned hero composition and did not alter its layout or visual content.
- Verified lint, production build, and whitespace checks.

## NEXT
- Define the first release-permission model and default autonomy boundaries.
- Implement the early-access capture destination or replace the mailto call to action when the onboarding route exists.
- Design and implement the MVP Orb creation and Nursery flow from the product docs.
- A valid `.riv` animation still requires a Rive editor/CLI or existing Rive source. This repository has no Rive dependency, authoring tool, or source file; do not add a placeholder `.riv`.
- When revisiting the landing hero, treat the current `landing-hero.component.tsx` state as user-owned unless the user explicitly asks to modify it.
- Use `var(--font-display)` / `var(--font-pixel)` for headings and `var(--font-mono)` for compact themed labels.
- `AnimatedOrb` depends on `orb-sphere.png` and `orb-vertical-flare.png`; retain their identical square dimensions so they stay aligned.

## BLOCKERS
No blocking issue. A valid `.riv` animation still requires a Rive editor/CLI or source file; do not add a placeholder `.riv`.

## TOUCHED
- web/package.json
- web/pnpm-lock.yaml
- web/src/app/page.tsx
- web/src/components/landing-scroll-motion.client.tsx
- web/src/components/landing-story.component.tsx
- web/src/components/landing-activity.component.tsx
- web/src/components/landing-world.component.tsx
- web/src/components/landing-autonomy.component.tsx
- web/src/components/landing-call-to-action.component.tsx
- .agent/shared-mind.txt

## COMMANDS
- `pnpm add gsap` (in `web/`) — pass
- `pnpm lint` (in `web/`) — pass
- `pnpm build` (in `web/`) — pass
- `git diff --check` — pass
- `.agent/scripts/agen-run.sh .agent/post-agent-run.acmd` — pass

## REPO MAP
- `app/` — Capacitor + Vite mobile (Frontend.Mobile.RULES.md)
- `server/` — Express API (Server.RULES.md)
- `web/` — Next.js (Frontend.Web.RULES.md)
- `.agent/project/` — product docs (templates — fill in)
- `.agent/` — code rules, shared-mind, Agen post-run

## CONVENTIONS REMINDER
- `.agent/` = all agents | `.cursor/` = Cursor only (rules + hooks)
- Post-run: update this file, then `.agent/scripts/agen-run.sh .agent/post-agent-run.acmd`
